<p id="notice"><%= notice %></p>

<h1>Reviews</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th>Rating</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reviews.each do |review| %>
      <tr>

        <!-- <td><%= review.body %></td>
        <td><%= review.rating %></td>
        <td><%= review.title %></td> -->
        <td><%= link_to 'Show', user_review_path(@user, review) %></td>
        <td><%= link_to 'Edit', edit_user_review(@user, review) %></td>
        <td><%= link_to 'Destroy', [@user, review], confirm: 'Are you sure?', method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Review', new_user_review_path(@user) %>
